We can work on the same project and make changes without truly editing the master branch. To do this, you can create a ‘version 2’ or different branch. This helpful to edit prior to merging to the master. 
We can work on the same project and make changes without truly editing the master branch. To do this, you can create a ‘version 2’ or different branch. This helpful to edit prior to merging to the master. 

